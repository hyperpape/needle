package com.justinblank.strings;

import java.util.Optional;

public class RegexParser {

    public static Optional<NFA> parse(String regex) {
        for (int i = 0; i < regex.length(); i++) {
            char c = regex.charAt(i);
        }
        return null;
    }
}
